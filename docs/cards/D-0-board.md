# Card D-0 — Fluency board

**Milestone:** M8 · **Lane:** docs · **Depends:** C-0 · **Status:** landed — C Close is archive; this README is the D board

## Handoff

- **Landed:** D-1–D-4 written. Fluency is a language budget, not a
  new silhouette. ROADMAP has M8. B-X “maybe later” now points at
  cards. Save a scaled recipe stays named, not a pick, until a
  cook asks. C-1 GUI sit stays open. C-3 landed (ad-hoc `.app`).
- **Not this card:** whispering the editor (D-1), prose Read (D-2),
  resolving `@./sauces/…` (D-3), ingredient search (D-4), sitting
  the window (C-1), a signed app (C-3).

## Owns

- `docs/cards/README.md` and the D-*.md briefs
- Status lines that move fluency off “do not start”
- `docs/STATUS.md`, `docs/ROADMAP.md`
- `CHANGELOG.md` Unreleased line
- Pointers in `README.md`, `docs/ARCHITECTURE.md`, and `B-X-refuse.md`

## Do not touch

- Swift sources
- Inventing an E (stylesheets, tags filter, a fourth pane) as work
- Growing `docs/THESIS.md`
- Starting D-1 in this PR

## Why

C-0 closed the silhouette and forbade a D board so Close could
finish. Close is finishing: C-2 landed, C-1 has a script, C-3 is
ready. Leaving fluency as a bullet list makes the next session
invent a preview column. Fluency is the thesis paradox — look
simple, be fluent — and it needs the same path contract as B and C.

## Do

1. Archive C-0–C-3. Keep C-1 and C-3 visible as Close leftovers.
2. Write D-1, D-2, D-3, D-4. No E cards. No tags-as-place.
3. STATUS: silhouette still the product; fluency is the next
   language work, gated on the C-1 sit.
4. ROADMAP M8 is Fluency, not a second SSG or a Kitchen Mode.
5. B-X: whispered markup, prose Read, sauce walk, and ingredient
   search point at D cards. Scaled-recipe save stays “if a cook
   asks.”

## Do not

- Add a column, a pane, or a preview “while we are in the docs.”
- Tell anyone to start D-1 before a human has sat C-1.
- Promote stylesheets, Vim mode, or All Recipes into this board.

## Gate

Someone who only reads `docs/cards/README.md` knows the next
*feature* session is D-1 after the sit — or C-3 if they are
handing over an `.app`. Dinner and a paragraph are still the
same window.
